<h1>Listing features</h1>
<%= link_to 'New feature', new_feature_path %>
<table>

  <tr>
    <th>Title</th>
    <th>Text</th>
  </tr>

  <% @features.each do |feature| %>
    <tr>
      <td><%= feature.title %></td>
      <td><%= feature.text %></td>
    </tr>
  <% end %>

</table>